import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bidspark.emdtracker',
  appName: 'BidSpark EMD Tracker',
  webDir: 'dist'
};

export default config;
